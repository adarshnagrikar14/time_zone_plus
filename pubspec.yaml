name: time_zone_plus
description: "A Flutter package to get timezone names from UTC offsets for current location of User."
version: 0.0.1
homepage: https://github.com/adarshnagrikar14/time_zone_plus

environment:
  sdk: ^3.5.3
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  timezone: ^0.9.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter: